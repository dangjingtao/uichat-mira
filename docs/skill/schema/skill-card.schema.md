# Skill Card Schema（旧版，已废弃）

Status: Historical
Owner: docs / chat / runtime
Last verified: 2026-07-22
Layer: raw-source
Module: SKILL
Feature: SkillCardSchema
Doc Type: historical
Canonical: false
Related:
  - ../README.md
  - ../skill-runtime-design.md

## 状态

这份 schema 属于旧的 memory-skill card POC，已停止作为当前 Skill 定义或实现合同。

当前正式定义：

> `Skill = 内部状态 + 多工具编排 + 业务语义封装`。

新设计不继续沿用旧 `skill card` schema。`SkillDefinition`、`SkillInstance` 和运行时合同见：

- `../README.md`
- `../skill-runtime-design.md`

需要旧 schema 细节时查看 Git 历史。
