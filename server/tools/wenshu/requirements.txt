# Installed on demand into Mira's managed `wenshu-office` runtime pack.
# The system development-kit Python is reused as the interpreter; these packages
# are not installed into the user's global Python environment.
reportlab>=4.0
matplotlib>=3.8
pdfplumber>=0.11
pikepdf>=9.0
markdown2>=2.5
xhtml2pdf>=0.2.17
openpyxl>=3.1
python-pptx>=0.6.21
Pillow>=10.0
# Kimi PPT DSL runtime dependencies.
lxml>=4.9.0
requests>=2.28.0
ruamel.yaml>=0.18.0
latex2mathml>=3.77.0
fonttools>=4.0
# Optional spreadsheet recalculation provider used by xlsx_tools.py:
# formulas>=1.2
