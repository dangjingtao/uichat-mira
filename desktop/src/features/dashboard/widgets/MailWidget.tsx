import { Mail } from "lucide-react";
import type { DashboardWidget, MailData } from "../types/dashboard-types";
import { WidgetCard } from "../components/WidgetCard";
export function MailWidget({ widget }: { widget: DashboardWidget<MailData> }) { return <WidgetCard widget={widget}><div className="mb-3 flex items-center gap-2 text-sm text-text-secondary"><Mail className="h-4 w-4 text-primary" /><span>未读邮件</span><strong className="text-text-primary">{widget.data.unreadCount}</strong></div><div className="space-y-3">{widget.data.items.map((item) => <div key={`${item.sender}-${item.subject}`} className="border-b border-border pb-3 last:border-0 last:pb-0"><div className="flex justify-between gap-3 text-xs"><span className="font-medium text-text-primary">{item.sender}</span><span className="shrink-0 text-text-tertiary">{item.receivedAt}</span></div><div className="mt-1 text-sm text-text-primary">{item.subject}</div><div className="mt-1 text-xs text-text-secondary">{item.preview}</div></div>)}</div></WidgetCard>; }
